#include "stdio.h"

using namespace std;

int main() {
    for (auto a: list) {
        if (a % 2 == 0) {
            for (auto i = 10; i >= 1; i = i + -1) {
                x = 1;
            }
        }
    }
}